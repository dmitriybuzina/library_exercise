$(".book-action").html(
    '<%= button_to "#{ @book.taken_by?(current_user.id) ? 'Return' : 'Busy' }", return_book_path(@book), id: 'action-book',
    method: :put, remote: true, class: 'btn btn-secondary' %>'
)
document.getElementById('book-histories').style.visibility = 'visible'
$("#book-histories table").html("<%= escape_javascript(render 'books/histories', histories: @histories ) %>")
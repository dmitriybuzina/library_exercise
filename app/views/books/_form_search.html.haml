-#=form_with(url: search_books_path, method: :get) do |form|
-#  =form.text_field :search, value: params[:search]
-#  =form.submit 'Search'

= form_tag search_books_path, :method => 'get' do
  %p
    = text_field_tag :search, params[:search]
    = submit_tag "Search", :title => nil
